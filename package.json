{
  "name": "telegram-wordle-game-static-client",
  "version": "1.0.0",
  "description": "Minimal package.json to host static files on Vercel and pass JSON validation.",
  "scripts": {
    "start": "echo 'Static files deployed.'"
  }
}
